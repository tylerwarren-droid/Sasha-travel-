# Session State — 07/08/2026, 05:53 AM

_Auto-captured by Claude Thread_

## Summary
Session captured

{"summary": "This is the first captured session for the Sasha/Kanoe AI travel concierge project — no live conversation transcript exists, so the handoff is built entirely from repo context, README, and the auto-generated session_current.md. The most critical open item is a documented file-location discrepancy: docs claim the conductor lives at backend/app/api/conductor.py while the real orchestration logic is in backend/app/services/conductor.py, and there is a model-name inconsistency (docs say Haiku, code uses claude-sonnet-4-5). The repo is also currently PUBLIC for HeyGen review and must be made private once resolved.", "session_doc": "# ════════════════════════════════════════════════════════════\n# SASHA / KANOE — AI TRAVEL CONCIERGE — THREAD CONTEXT\n# The single source of truth. Read this completely before doing anything.\n# Generated: 2026-06-13 · Model: Sonnet · Session #1 · ~1100 tokens\n# ════════════════════════════════════════════════════════════\n\n## ⚡ READ THIS FIRST — 30 SECOND ORIENTATION\nKanoe is a multi-agent AI travel platform whose voice concierge, \"Sasha,\" runs on a HeyGen LiveAvatar with a self-owned Deepgram STT mic pipeline and 23 backend specialist agents. Today the platform is live across demo/investor/Vietnam/Phu Quoc URLs, the voice pipeline works, but the docs and code disagree on where the conductor (orchestrator) lives and which Claude model the agents actually use. The single most important thing right now is to resolve that conductor file discrepancy before editing any dispatch logic, and to make the repo private once HeyGen review is done.\n\n> NEXT ACTION: In the repo github.com/tylerwarren-droid/Sasha-travel-, open BOTH backend/app/api/conductor.py and backend/app/services/conductor.py. Determine which one is the live dispatcher imported by backend/app/main.py (grep for `conductor` imports in main.py and app/api/*.py). Confirm the real orchestration `conduct()` function lives in services/conductor.py, then update docs/new_chat_handoff.md to (a) correct the conductor path and (b) fix the false claim that agents use `claude-haiku-4-5` — `run_general()` and the merge step in services/conductor.py actually use `claude-sonnet-4-5`. Verify by re-reading the corrected handoff against the code.\n> DO NOT: Do not \"fix\" the model reference by forcing all agent calls to Haiku to match the old doc — the code deliberately uses claude-sonnet-4-5 in run_general (max_tokens=300) and the merge step (max_tokens=400). The doc is wrong, not the code.\n\n# ─────────────────────────────────────────────────────────────\n# PART 1 — WHAT THIS IS (the unchanging core)\n# ─────────────────────────────────────────────────────────────\n\n## THE ESSENCE — never let this drift\nKanoe is a multi-agent AI travel concierge platform. The one thing it must always do well: deliver a natural, low-latency VOICE conversation with an on-screen video avatar (Sasha) that dispatches user requests to 23 specialist agents (golf, visa, restaurants, transfers, etc.) and speaks answers back through the HeyGen avatar. The crown jewel — and most fragile part — is the voice pipeline: HeyGen LiveAvatar (video + TTS, `voiceChat: false` so WE own the mic) → Deepgram nova-3 AudioWorklet STT → conductor → response → avatar.repeat(). Any change that degrades turn-taking, echo suppression, or the gate/barge-in behavior compromises the essence and must be rejected.\n\n## WHO IT'S FOR\nThe repo owner/developer is tylerwarren-droid (GitHub handle). End users are luxury/high-end travelers using demos like /vietnam and /phuquoc. There is an investor-facing track (investor.kanoe.ai, sasha_investor.html, Kanoe Investor Deck) — meaning polish and reliability matter for fundraising, not just internal testing. HeyGen is currently reviewing the repo (it was made public for that review). Technical level: developer-competent, works via Claude picking up cold from docs/session_current.md; expects Claude to read the docs first and continue without re-explaining.\n\n# ─────────────────────────────────────────────────────────────\n# PART 2 — WHAT'S TRUE RIGHT NOW (the current reality)\n# ─────────────────────────────────────────────────────────────\n\n## CURRENT STATE — what actually exists vs what's aspirational\n- WORKING NOW (verified): Live URLs deployed — investor.kanoe.ai, demo.kanoe.ai, sasha-heygen.vercel.app/vietnam, sasha-heygen.vercel.app/phuquoc, backend at sasha-travel-production.up.railway.app. Voice pipeline (tap-to-start → HeyGen avatar → Deepgram STT → conductor → avatar.repeat) is documented as functioning with echo filtering, dedup, gate/barge-in. 23 backend agents exist as service modules. DB writes wired (commit dcb5b3f).\n- BUILT BUT UNVERIFIED: The /vietnam2 \"Luxurious Traveler UI\" with Leaflet map (recent commits 6461b4c / 990f01f) — new, not confirmed tested. Onboarding flow (frontend/app/onboarding, 6 steps) exists but no test evidence. Investor deck updates (commit 012bdd0) present. The exact live conductor file/dispatch path is UNVERIFIED (see discrepancy below).\n- ASPIRATIONAL / NOT BUILT: Making the repo private (still PUBLIC). Any AgAPI / Dataset Licensing / TDM roadmap items referenced in the investor deck are strategy/pitch, not necessarily shipped features. Do not assume investor-deck pillars map to working code.\n\n## LAST SESSION — what just happened\n- We worked on: This is Session #1 with no captured conversation transcript (auto-capture / pre-buffer session). Context is reconstructed from repo state and the auto-generated docs/session_current.md dated 2026-06-13 15:51.\n- We completed: Recent commit history shows investor deck full update (012bdd0), MOR corpus verbiage + Data Licensing pillar fix (26b27bf, latest), project.kanoe.ai redirect fix (ad49a8b), teaser updates (e5a5622), and the /vietnam2 luxury UI build (6461b4c).\n- We changed: Latest commit 26b27bf touched investor/marketing content. Uncommitted change present: a new binary file `frontend/public/slides/Kanoe_Investor_Deck_May 2026 7.key` (untracked, `??`).\n- We left off mid-: No half-finished code work is captured. The outstanding known issue is the documented conductor-location + model-name discrepancy in session_current.md's \"Critical Discrepancy\" section, which was flagged but NOT resolved. The untracked .key slide file is also uncommitted.\n\n## OPEN TASKS — ranked, with the WHY\n1. Resolve the conductor file discrepancy — matters because editing the wrong conductor file will silently do nothing or break dispatch — verify which file main.py imports before ANY orchestration edit; then correct docs/new_chat_handoff.md.\n2. Fix the Haiku-vs-Sonnet doc inconsistency — matters because the doc misleads future Claude into changing model config incorrectly — update the doc to match the code (agents use claude-sonnet-4-5 in run_general and merge), do NOT change the code.\n3. Make the repo PRIVATE once HeyGen review resolves — matters because it's currently PUBLIC and contains a full commercial travel platform + investor material — confirm HeyGen review status first, then flip visibility in GitHub settings.\n4. Decide/commit the untracked `Kanoe_Investor_Deck_May 2026 7.key` — matters because it's an uncommitted binary cluttering the working tree — either commit it with a clear message or add to .gitignore.\n\n# ─────────────────────────────────────────────────────────────\n# PART 3 — THE GUARDRAILS (what stops drift and repetition)\n# ─────────────────────────────────────────────────────────────\n\n## ✋ DO NOT TOUCH — load-bearing, deliberate, leave alone\n- `voiceChat: false` in SashaAvatar.tsx LiveAvatarSession config — looks like a feature toggle you could enable, but is deliberately OFF because WE own the entire mic pipeline (Deepgram). Enabling HeyGen voiceChat would double-capture the mic and break echo/gate logic. Do not change.\n- `micGatedRef = useRef(true)` starting GATED in VoiceButton.tsx — looks like a bug (mic seems dead at start) but is deliberate: the worklet runs immediately, audio is suppressed until the first `AVATAR_SPEAK_ENDED`. The gate is a pure ref flag with no pipeline pause/resume — intentional. Do not \"fix.\"\n- No auto-reconnect on `ws.onclose` in VoiceButton.tsx — looks like a missing resilience feature, but is intentional (see Gotchas in session doc). Do not add auto-reconnect.\n- The `speakHandlersRegistered` guard in SashaAvatar.tsx (registers speak handlers exactly once) — this was BUG 3; removing the guard re-introduces duplicate handler registration. Leave it.\n- Dedup scoped per-connection (`lastFinal`/`lastFinalAt`) — this was BUG 4. Do not make it global/persistent across reconnects.\n- StrictMode guard in `ws.onopen` (`if wsRef.current !== ws` discard) — deliberate to survive React StrictMode double-mount. Do not remove.\n- Keepalive intervals: HeyGen keepAlive every 150000ms (150s), Deepgram KeepAlive every 8s — tuned values, not arbitrary. Do not casually change.\n\n## 🚫 ALREADY DECIDED — do not re-open these\n| Decision | Why we chose it | What we rejected | Reversible? |\n|----------|-----------------|------------------|-------------|\n| We own the mic pipeline (Deepgram nova-3 AudioWorklet), HeyGen `voiceChat:false` | Full control over echo suppression, gating, barge-in | Letting HeyGen handle voice chat | No |\n| Conductor uses claude-sonnet-4-5 for run_general (max 300) + merge (max 400) | Quality of orchestration/merge output | Haiku for all agent loops (docs wrongly claim this) | Yes (but code is source of truth) |\n| Session handoff via docs/session_current.md regenerated by scripts/end_session.sh calling Claude API | Consistent cold-start onboarding | Manual notes | Yes |\n| Tap-to-start overlay gates all audio init | Browser autoplay/mic gesture requirement | Auto-init on load | No |\n\n## 💀 TRIED AND FAILED — do not suggest these again\n- Registering HeyGen speak handlers without a guard — caused duplicate registrations (BUG 3). Fixed with `speakHandlersRegistered`. Do not retry unguarded registration.\n- Global/unscoped final-transcript dedup — caused issues across connections (BUG 4). Now scoped per connection. Do not revert to global.\n- (No other failed approaches captured — this is Session #1.)\n\n## 🧭 USER'S STANDING PREFERENCES — how they want things done\n- Start every new
---
_Generated: 2026-07-08T03:53:44.268Z

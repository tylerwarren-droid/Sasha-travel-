# Session State — 07/08/2026, 06:52 AM

_Auto-captured by Claude Thread_

## Summary
Session captured

{"summary": "This is Session #1 of the Sasha/Kanoe AI travel concierge handoff, reconstructed entirely from repo context, README, and the auto-generated session_current.md since no live conversation transcript exists. The single most critical open item is a documented file-location discrepancy — docs claim the conductor lives at backend/app/api/conductor.py while the real orchestration logic is in backend/app/services/conductor.py — plus a model-name inconsistency where docs say Haiku but the code uses claude-sonnet-4-5. The repo is also currently PUBLIC for HeyGen review and must be made private once these are resolved.", "session_doc": "# ════════════════════════════════════════════════════════════\n# SASHA / KANOE — AI TRAVEL CONCIERGE — THREAD CONTEXT\n# The single source of truth. Read this completely before doing anything.\n# Generated: 2026-07-08 · Model: Sonnet · Session #1 · ~1100 tokens\n# ════════════════════════════════════════════════════════════\n\n## ⚡ READ THIS FIRST — 30 SECOND ORIENTATION\nKanoe is a multi-agent AI travel platform whose voice concierge, \"Sasha,\" runs on a HeyGen LiveAvatar with a self-owned Deepgram STT mic pipeline and 23 backend specialist agents. Today the platform is live across demo/investor/Vietnam/Phu Quoc URLs and the voice pipeline works, but the docs and code disagree on where the conductor (orchestrator) lives and which Claude model the agents actually use. The single most important thing right now is to resolve that conductor file discrepancy in the docs before editing any dispatch logic, and to make the repo private once HeyGen review is done.\n\n> NEXT ACTION: In repo github.com/tylerwarren-droid/Sasha-travel-, run `grep -rn \"conductor\" backend/app/main.py backend/app/api/*.py` to identify which conductor module main.py actually imports. Open BOTH backend/app/api/conductor.py and backend/app/services/conductor.py, confirm the live orchestration `conduct()` / `run_general()` function is in services/conductor.py, then edit docs/new_chat_handoff.md to (a) correct the conductor path from api/conductor.py to services/conductor.py and (b) fix the false claim that agents use claude-haiku-4-5 — replace with claude-sonnet-4-5 (used in run_general max_tokens=300 and the merge step max_tokens=400). Verify by re-reading the corrected handoff line-by-line against services/conductor.py.\n> DO NOT: Do not \"fix\" the model reference by forcing all agent calls to Haiku to match the old doc — the code deliberately uses claude-sonnet-4-5 in run_general (max_tokens=300) and the merge step (max_tokens=400). The DOC is wrong, not the code.\n\n# ─────────────────────────────────────────────────────────────\n# PART 1 — WHAT THIS IS (the unchanging core)\n# ─────────────────────────────────────────────────────────────\n\n## THE ESSENCE — never let this drift\nKanoe is a multi-agent AI travel concierge platform. The one thing it must always do well: deliver a natural, low-latency VOICE conversation with an on-screen video avatar (Sasha) that dispatches user requests to 23 specialist agents (golf, visa, restaurants, transfers, etc.) and speaks answers back through the HeyGen avatar. The crown jewel — and most fragile part — is the voice pipeline: HeyGen LiveAvatar (video + TTS, `voiceChat: false` so WE own the mic) → Deepgram nova-3 AudioWorklet STT → conductor → response → avatar.repeat(). Any change that degrades turn-taking, echo suppression, or the gate/barge-in behavior compromises the essence and must be rejected.\n\n## WHO IT'S FOR\nThe repo owner/developer is tylerwarren-droid (GitHub handle). End users are luxury/high-end travelers using demos like /vietnam and /phuquoc. There is an investor-facing track (investor.kanoe.ai, sasha_investor.html, Kanoe Investor Deck) — meaning polish and reliability matter for fundraising, not just internal testing. HeyGen is currently reviewing the repo (it was made public for that review). Technical level: developer-competent, works via Claude picking up cold from docs/session_current.md; expects Claude to read the docs first and continue without re-explaining.\n\n# ─────────────────────────────────────────────────────────────\n# PART 2 — WHAT'S TRUE RIGHT NOW (the current reality)\n# ─────────────────────────────────────────────────────────────\n\n## CURRENT STATE — what actually exists vs what's aspirational\n- WORKING NOW (verified): Live URLs deployed — investor.kanoe.ai, demo.kanoe.ai, sasha-heygen.vercel.app/vietnam, sasha-heygen.vercel.app/phuquoc, backend at sasha-travel-production.up.railway.app. Voice pipeline (tap-to-start → HeyGen avatar → Deepgram STT → conductor → avatar.repeat) is documented as functioning with echo filtering, dedup, gate/barge-in. 23 backend agents exist as service modules. DB writes wired (commit dcb5b3f).\n- BUILT BUT UNVERIFIED: The /vietnam2 \"Luxurious Traveler UI\" with Leaflet map (commits 6461b4c / 990f01f) — new, not confirmed tested. Onboarding flow (frontend/app/onboarding, 6 steps: Account/Pathway/Prompt/Inventory/Agents/Deploy) exists but no test evidence. Investor deck updates (commit 012bdd0) present. The exact live conductor file/dispatch path is UNVERIFIED (see discrepancy below).\n- ASPIRATIONAL / NOT BUILT: Making the repo private (still PUBLIC for HeyGen review). Any AgAPI / Dataset Licensing / TDM / MOR corpus roadmap items referenced in the investor deck are strategy/pitch, not necessarily shipped features. Do not assume investor-deck pillars map to working code.\n\n## LAST SESSION — what just happened\n- We worked on: This is Session #1 with no captured conversation transcript (auto-capture / pre-buffer session). Context is reconstructed from repo state and the auto-generated docs/session_current.md dated 07/08/2026, 05:53 AM.\n- We completed: Recent commit history shows investor deck full update (012bdd0), MOR corpus verbiage + Data Licensing pillar fix (26b27bf, latest committed), project.kanoe.ai redirect fix (ad49a8b), teaser updates (e5a5622), and the /vietnam2 luxury UI build (6461b4c).\n- We changed: Latest commit 26b27bf touched investor deck / MOR corpus verbiage and the Data Licensing pillar. There is one uncommitted file: \"frontend/public/slides/Kanoe_Investor_Deck_May 2026 7.key\" (untracked, `??`).\n- We left off mid-: No active in-progress code edit. The open thread is the documented conductor.py path discrepancy and model-name mismatch in docs/new_chat_handoff.md, plus the still-untracked investor deck .key file, plus the repo still being PUBLIC.\n\n## OPEN TASKS — ranked, with the WHY\n1. Fix conductor path + model-name in docs/new_chat_handoff.md — matters because incorrect docs will cause the next Claude to edit the wrong file (api/conductor.py) or downgrade models to Haiku, breaking dispatch — do it by verifying against main.py imports and services/conductor.py, editing docs only, not code.\n2. Make the repo private once HeyGen review is complete — matters because the repo is PUBLIC and contains proprietary orchestration + business IP — constraint: confirm HeyGen no longer needs access first.\n3. Commit or discard the untracked \"frontend/public/slides/Kanoe_Investor_Deck_May 2026 7.key\" — matters because uncommitted binary state clutters status and may be lost — decide if it's the canonical deck.\n4. Verify /vietnam2 Luxurious Traveler UI and 6-step onboarding flow actually work end-to-end — matters because they are BUILT BUT UNVERIFIED and investor-facing.\n\n# ─────────────────────────────────────────────────────────────\n# PART 3 — THE GUARDRAILS (what stops drift and repetition)\n# ─────────────────────────────────────────────────────────────\n\n## ✋ DO NOT TOUCH — load-bearing, deliberate, leave alone\n- `voiceChat: false` in the HeyGen LiveAvatar config — looks like a disabled feature but is deliberate because WE own the mic pipeline (Deepgram AudioWorklet STT). Turning it on would double-capture audio and break echo suppression / turn-taking. Do not change.\n- claude-sonnet-4-5 model in services/conductor.py run_general (max_tokens=300) and merge step (max_tokens=400) — looks like it contradicts the docs' \"Haiku\" claim but the CODE is correct and deliberate. Do not downgrade to Haiku. Fix the doc instead.\n- backend/app/services/conductor.py — this is the REAL orchestrator (not api/conductor.py). Do not assume the api-layer file is the dispatcher; verify imports first.\n- The voice pipeline echo filtering / dedup / gate / barge-in logic — fragile and hand-tuned. Do not refactor for elegance.\n\n## 🚫 ALREADY DECIDED — do not re-open these\n| Decision | Why we chose it | What we rejected | Reversible? |\n|----------|-----------------|------------------|-------------|\n| Own the mic via Deepgram AudioWorklet (`voiceChat: false`) | Full control of STT, echo suppression, barge-in | Letting HeyGen handle voiceChat | No |\n| claude-sonnet-4-5 for conductor/agent general reasoning | Quality of dispatch/merge over cheapest model | claude-haiku-4-5 (docs incorrectly claim this) | Reversible but not now |\n| Next.js App Router + React 19 + TS + Tailwind on Vercel | Existing stack, deployed | — | No |\n| FastAPI on Railway, Supabase Postgres w/ RLS | Existing stack, deployed | — | No |\n| Repo temporarily PUBLIC | Required for HeyGen review | Keeping private during review | Yes — revert to private after review |\n\n## 💀 TRIED AND FAILED — do not suggest these again\n- Not yet established (no failed-approach history captured in this first session — do not fabricate one).\n\n## 🧭 USER'S STANDING PREFERENCES — how they want things done\n- Sessions start by pasting: \"Read docs/session_current.md and the files it references in https://github.com/tylerwarren-droid/Sasha-travel- then pick up where we left off.\" — read docs FIRST, continue without re-explaining.\n- End-of-session state is regenerated via `ANTHROPIC_API_KEY=your_key ./scripts/end_session.sh` which calls Claude and auto-commits.\n- Documentation is treated as living truth — when code and docs conflict, correct the docs (unless the code is genuinely wrong).\n- Commits use conventional-style prefixes: feat:, fix:, chore: with [auto] tag for session captures.\n\n# ─────────────────────────────────────────────────────────────\n# PART 4 — THE MAP (how it's all put together)
---
_Generated: 2026-07-08T04:52:47.793Z

# Session State — 07/08/2026, 10:40 AM

_Auto-captured by Claude Thread_

## Summary
Session captured

{"summary": "This session focused on the investor-facing deck (frontend/public/sasha_investor.html). Two new slides (Agent Architecture + Token Optimisation) were first injected between Competitive Landscape and Financial Projections, but the user asked to relocate them into the TDM deck as a single combined 'Performance Architecture' slide (now tdm-s11). The session cut off mid-task while working on two remaining requests: (1) designing an 'API Hub' architecture where system APIs coordinate amongst the agents, and (2) removing the $1M fundraising boxes from BOTH the Teaser tab and Investor Deck tab — Claude had just started grepping for those boxes when the response failed.", "session_doc": "# ════════════════════════════════════════════════════════════\n# SASHA / KANOE — AI TRAVEL CONCIERGE — THREAD CONTEXT\n# The single source of truth. Read this completely before doing anything.\n# Generated: 2026-07-08 · Model: Sonnet · Session #2 · ~1100 tokens\n# ════════════════════════════════════════════════════════════\n\n## ⚡ READ THIS FIRST — 30 SECOND ORIENTATION\nKanoe is a multi-agent AI travel platform whose voice concierge \"Sasha\" runs on a HeyGen LiveAvatar with a self-owned Deepgram STT mic pipeline and 23 backend specialist agents. This session was NOT about the voice pipeline — it was pure investor-deck work on the single HTML file frontend/public/sasha_investor.html (served at investor.kanoe.ai). The single most important thing right now: finish the two in-flight deck edits — (1) remove the $1M fundraising boxes from BOTH the Teaser tab and the Investor Deck tab, and (2) add an 'API Hub' architecture slide/concept — because the session died mid-grep for the $1M boxes.\n\n> NEXT ACTION: In frontend/public/sasha_investor.html run `grep -n \"1 million\\|\\$1M\\|1,000,000\\|raise\" frontend/public/sasha_investor.html` to locate BOTH fundraising boxes (one in the Teaser tab, one in the Investor Deck tab). Delete both box <div> blocks entirely (the user wants that info shared separately, NOT relocated). Then download the edited file and deploy with: `cp ~/Downloads/sasha_investor.html frontend/public/sasha_investor.html && git add . && git commit -m \"Remove $1M fundraising boxes from Teaser and Investor Deck tabs\" && git push origin main`. Verify by loading investor.kanoe.ai on both the Teaser tab and Investor Deck tab and confirming NO $1M/raise box appears in either.\n> DO NOT: Do not re-inject the Agent Architecture / Token Optimisation content as standalone slides between Competitive Landscape and Financial Projections — the user explicitly rejected that placement and had it merged into the TDM deck as slide tdm-s11 'Performance Architecture'. Leave that slide where it is.\n\n# ─────────────────────────────────────────────────────────────\n# PART 1 — WHAT THIS IS (the unchanging core)\n# ─────────────────────────────────────────────────────────────\n\n## THE ESSENCE — never let this drift\nKanoe is a multi-agent AI travel concierge platform. The one thing it must always do well: deliver a natural, low-latency VOICE conversation with an on-screen video avatar (Sasha) that dispatches user requests to 23 specialist agents (golf, visa, restaurants, transfers, etc.) and speaks answers back through the HeyGen avatar. The crown-jewel voice pipeline: HeyGen LiveAvatar (video + TTS, `voiceChat: false` so WE own the mic) → Deepgram nova-3 AudioWorklet STT → conductor → response → avatar.repeat(). ALONGSIDE the product itself, the investor deck (sasha_investor.html) is a first-class artifact because the company is fundraising — deck polish directly serves the essence of getting Kanoe funded.\n\n## WHO IT'S FOR\nThe repo owner/developer is tylerwarren-droid (GitHub handle; terminal user 'tylerwarren' on Mac-mini). End users are luxury/high-end travelers using demos like /vietnam and /phuquoc. There is an active investor-facing track (investor.kanoe.ai) — the deck matters for fundraising. Technical level: developer-competent, works from a Mac terminal, deploys via git push to Vercel. He has clear opinions on slide placement and content framing, and expects Claude to produce a downloadable HTML file plus the exact cp/git/push command sequence.\n\n# ─────────────────────────────────────────────────────────────\n# PART 2 — WHAT'S TRUE RIGHT NOW (the current reality)\n# ─────────────────────────────────────────────────────────────\n\n## CURRENT STATE — what actually exists vs what's aspirational\n- WORKING NOW (verified): Live URLs deployed — investor.kanoe.ai, demo.kanoe.ai, sasha-heygen.vercel.app/vietnam, /phuquoc, backend at sasha-travel-production.up.railway.app. sasha_investor.html has a Teaser tab and an Investor Deck tab plus a 10+ slide TDM deck navigated by ← → buttons. A new TDM slide 'Performance Architecture' (tdm-s11) was added this session combining agent orchestration flow (left) + three-level intelligence model & LLM routing cost table (right); nav counter was updated to match.\n- BUILT BUT UNVERIFIED: The tdm-s11 'Performance Architecture' slide was committed/deployed but not visually confirmed live by the user in this session. The /vietnam2 Leaflet UI and 6-step onboarding flow exist but lack test evidence.\n- ASPIRATIONAL / NOT BUILT: The 'API Hub' architecture (system APIs coordinating amongst agents) — user requested a DESIGN for it this session; nothing built yet. Removal of the $1M fundraising boxes — requested but NOT done (session died mid-grep). Making the repo private (still PUBLIC for HeyGen review).\n\n## LAST SESSION — what just happened\n- We worked on: Investor deck edits in frontend/public/sasha_investor.html only.\n- We completed: (1) Built two slides (Agent Architecture + Token Optimisation), first placed between Competitive Landscape and Financial Projections; (2) per user feedback, REMOVED that placement and instead merged both into a single new TDM deck slide tdm-s11 'Performance Architecture', updating the nav counter; committed with 'Add Performance Architecture slide to TDM deck'.\n- We changed: frontend/public/sasha_investor.html (added tdm-s11, updated TDM nav count). Deploy pattern: file is generated in Claude's container, user downloads from the chat link to ~/Downloads, then cp → git add → commit → push origin main (auto-deploys via Vercel).\n- We left off mid-: removing the $1M fundraising boxes. Claude ran 'Find the $1M fundraising boxes in both panels' and then the response failed ('Claude couldn't finish this response'). The two-part request (design API Hub architecture + remove both $1M boxes) is UNFINISHED.\n\n## OPEN TASKS — ranked, with the WHY\n1. Remove the $1M fundraising boxes from BOTH the Teaser tab and Investor Deck tab of sasha_investor.html — matters because the user wants to share the raise amount separately/privately, not publicly on the deck — DELETE the boxes, do not relocate them.\n2. Design and add an 'API Hub' architecture concept/slide — matters because the user wants to show that APIs within the system engage with and coordinate amongst the agents; likely belongs in or near the TDM/Performance Architecture area given precedent.\n3. Visually verify the tdm-s11 'Performance Architecture' slide renders correctly live at investor.kanoe.ai — matters because it was deployed but never confirmed by eye.\n4. (Carried from Session #1) Reconcile docs vs code: conductor lives in backend/app/services/conductor.py (not api/conductor.py per old docs); model is claude-sonnet-4-5 not Haiku — fix docs/new_chat_handoff.md.\n5. Make the repo private once HeyGen review is complete.\n\n# ─────────────────────────────────────────────────────────────\n# PART 3 — THE GUARDRAILS (what stops drift and repetition)\n# ─────────────────────────────────────────────────────────────\n\n## ✋ DO NOT TOUCH — load-bearing, deliberate, leave alone\n- The tdm-s11 'Performance Architecture' slide placement inside the TDM deck — looks like it could be a standalone top-level slide, but it is deliberately inside the TDM deck (navigated by the same ← → buttons) because the user explicitly said the technical detail belongs in the TDM. Do not move it.\n- The voice pipeline (HeyGen `voiceChat: false`, Deepgram AudioWorklet STT, gate/barge-in/echo filtering) — untouched this session; do not modify it while doing deck work.\n- backend/app/services/conductor.py — the real orchestration logic; do not 'fix' it to match outdated docs.\n\n## 🚫 ALREADY DECIDED — do not re-open these\n| Decision | Why we chose it | What we rejected | Reversible? |\n|----------|-----------------|------------------|-------------|\n| Put agent-arch + token-opt content in TDM deck as one 'Performance Architecture' slide | User said TDM is the right home for technical detail | Two separate slides between Competitive Landscape and Financial Projections | No |\n| Remove $1M raise boxes entirely (share separately) | User wants raise amount shared privately, not on public deck | Keeping the boxes / relocating them elsewhere on deck | Reversible but not desired |\n| Deploy via download → cp ~/Downloads → git add/commit/push origin main | Claude's container file isn't accessible from user's terminal | cp from /mnt/user-data/outputs (failed: No such file) | No |\n\n## 💀 TRIED AND FAILED — do not suggest these again\n- `cp /mnt/user-data/outputs/sasha_investor.html ...` — failed with 'No such file or directory' because the container path is not accessible from the user's local terminal — always tell the user to download the file from the chat link to ~/Downloads first, then cp from there.\n- Placing the technical slides between Competitive Landscape and Financial Projections — user disliked the placement; do not retry.\n\n## 🧭 USER'S STANDING PREFERENCES — how they want things done\n- Wants a downloadable HTML artifact PLUS the exact bash command block (cp ~/Downloads/... → git add → commit → push origin main) after every deck edit.\n- Has strong opinions on where content lives — asks for relocation rather than accepting default placement; confirm placement intent before big injections.\n- Prefers technical/architecture detail consolidated in the TDM section rather than scattered as standalone slides.\n- Direct communicator; expects Claude to read project knowledge/docs FIRST before acting (called this out earlier in session).\n\n# ─────────────────────────────────────────────────────────────\n# PART 4 — THE MAP (how it's all put together)\n# ─────────────────────────────────────────────────────────────\n\n## ARCHITECTURE / STRUCTURE\n- frontend/public/sasha_investor.html — LOAD-BEARING for this session. Single HTML file with a Teaser tab and an Investor Deck tab; the Investor Deck contains a TDM sub-deck (slides
---
_Generated: 2026-07-08T08:40:31.469Z

# Session State — 07/08/2026, 08:07 AM

_Auto-captured by Claude Thread_

## Summary
Session captured

{"summary": "This is Session #1 of the Sasha/Kanoe AI travel concierge handoff, reconstructed entirely from repo context, README, and the auto-generated session_current.md since no live conversation transcript exists. The single most critical open item is a documented file-location discrepancy — docs claim the conductor lives at backend/app/api/conductor.py while the real orchestration logic is in backend/app/services/conductor.py — plus a model-name inconsistency where docs say Haiku but the code uses claude-sonnet-4-5. The repo is also currently PUBLIC for HeyGen review and must be made private once these are resolved.", "session_doc": "# ════════════════════════════════════════════════════════════\n# SASHA / KANOE — AI TRAVEL CONCIERGE — THREAD CONTEXT\n# The single source of truth. Read this completely before doing anything.\n# Generated: 2026-07-08 · Model: Sonnet · Session #1 · ~1100 tokens\n# ════════════════════════════════════════════════════════════\n\n## ⚡ READ THIS FIRST — 30 SECOND ORIENTATION\nKanoe is a multi-agent AI travel platform whose voice concierge, \"Sasha,\" runs on a HeyGen LiveAvatar with a self-owned Deepgram STT mic pipeline and 23 backend specialist agents. Today the platform is live across demo/investor/Vietnam/Phu Quoc URLs and the voice pipeline works, but the docs and code disagree on WHERE the conductor (orchestrator) lives and WHICH Claude model the agents actually use. The single most important thing right now is to reconcile the docs with the real code — fix the conductor file path and the model name in docs/new_chat_handoff.md — before touching any dispatch logic, then make the repo private once HeyGen review is complete.\n\n> NEXT ACTION: In repo github.com/tylerwarren-droid/Sasha-travel-, run `grep -rn \"conductor\" backend/app/main.py backend/app/api/*.py` to confirm which conductor module main.py actually imports. Open BOTH backend/app/api/conductor.py and backend/app/services/conductor.py, verify the live orchestration function (conduct() / run_general()) is in services/conductor.py, then edit docs/new_chat_handoff.md to (a) correct the conductor path from api/conductor.py to services/conductor.py and (b) replace the false claim that agents use claude-haiku-4-5 with claude-sonnet-4-5 (used in run_general max_tokens=300 and the merge step max_tokens=400). Verify by re-reading the corrected handoff lines against services/conductor.py, then commit with message `docs: fix conductor path + model name in new_chat_handoff.md`.\n> DO NOT: Do not \"fix\" the model reference by forcing all agent calls to Haiku to match the old doc — the code deliberately uses claude-sonnet-4-5 in run_general (max_tokens=300) and the merge step (max_tokens=400). The DOC is wrong, not the code.\n\n# ─────────────────────────────────────────────────────────────\n# PART 1 — WHAT THIS IS (the unchanging core)\n# ─────────────────────────────────────────────────────────────\n\n## THE ESSENCE — never let this drift\nKanoe is a multi-agent AI travel concierge platform. The one thing it must always do well: deliver a natural, low-latency VOICE conversation with an on-screen video avatar (Sasha) that dispatches user requests to 23 specialist agents (golf, visa, restaurants, transfers, etc.) and speaks answers back through the HeyGen avatar. The crown jewel — and most fragile part — is the voice pipeline: HeyGen LiveAvatar (video + TTS, `voiceChat: false` so WE own the mic) → Deepgram nova-3 AudioWorklet STT → conductor → response → avatar.repeat(). Any change that degrades turn-taking, echo suppression, or gate/barge-in behavior compromises the essence and must be rejected.\n\n## WHO IT'S FOR\nThe repo owner/developer is tylerwarren-droid (GitHub handle). End users are luxury/high-end travelers using demos like /vietnam and /phuquoc. There is an investor-facing track (investor.kanoe.ai, sasha_investor.html, Kanoe Investor Deck) — meaning polish and reliability matter for fundraising, not just internal testing. HeyGen is currently reviewing the repo (it was made public for that review). Technical level: developer-competent; works via Claude picking up cold from docs/session_current.md; expects Claude to read the docs first and continue without re-explaining.\n\n# ─────────────────────────────────────────────────────────────\n# PART 2 — WHAT'S TRUE RIGHT NOW (the current reality)\n# ─────────────────────────────────────────────────────────────\n\n## CURRENT STATE — what actually exists vs what's aspirational\n- WORKING NOW (verified): Live URLs deployed — investor.kanoe.ai, demo.kanoe.ai, sasha-heygen.vercel.app/vietnam, sasha-heygen.vercel.app/phuquoc, backend at sasha-travel-production.up.railway.app. Voice pipeline (tap-to-start → HeyGen avatar → Deepgram STT → conductor → avatar.repeat) documented as functioning with echo filtering, dedup, gate/barge-in. 23 backend agent service modules exist (backend/app/services/*_agent.py). DB writes wired (commit dcb5b3f).\n- BUILT BUT UNVERIFIED: The /vietnam2 \"Luxurious Traveler UI\" with Leaflet map (commits 6461b4c / 990f01f) — new, not confirmed tested. Onboarding flow (frontend/app/onboarding, 6 steps: Account/Pathway/Prompt/Inventory/Agents/Deploy) exists but no test evidence. Investor deck updates (commit 012bdd0) present. The exact live conductor file/dispatch path is UNVERIFIED pending the grep in NEXT ACTION.\n- ASPIRATIONAL / NOT BUILT: Making the repo private (still PUBLIC for HeyGen review). AgAPI / Dataset Licensing / TDM / MOR corpus roadmap items referenced in the investor deck are strategy/pitch, not necessarily shipped features. Do not assume investor-deck pillars map to working code.\n\n## LAST SESSION — what just happened\n- We worked on: This is Session #1 with NO captured conversation transcript (auto-capture / pre-buffer session). Context is reconstructed entirely from repo state, README, and the auto-generated docs/session_current.md dated 07/08/2026, 06:52 AM.\n- We completed: Nothing was actively completed in a live session. Recent commit history shows: investor deck full update (012bdd0), MOR corpus verbiage + Data Licensing pillar fix (26b27bf), project.kanoe.ai redirect (ad49a8b), teaser updates (e5a5622), /vietnam2 Luxurious Traveler UI (6461b4c/990f01f), and DB writes + HeyGen echo fix wired (dcb5b3f).\n- We changed: No live edits this session. One uncommitted file present in working tree: `frontend/public/slides/Kanoe_Investor_Deck_May 2026 7.key` (untracked).\n- We left off mid-: Reconstructing context. Two unresolved documentation defects flagged: (1) conductor file path discrepancy (docs say api/conductor.py, code lives in services/conductor.py); (2) model-name mismatch (docs say Haiku, code uses claude-sonnet-4-5). Repo still PUBLIC awaiting HeyGen review completion.\n\n## OPEN TASKS — ranked, with the WHY\n1. Fix conductor path + model name in docs/new_chat_handoff.md — matters because a wrong file path will send the next Claude editing dead code (api/conductor.py) instead of live logic (services/conductor.py), and the Haiku/Sonnet mismatch could cause someone to \"correct\" working code the wrong way — constraint: edit the DOC, never force the code to match the old doc.\n2. Make the repo private — matters because it was only made public for HeyGen review; leaving it public exposes source, keys patterns, and investor materials — constraint: only after HeyGen review is confirmed complete.\n3. Verify /vietnam2 Luxurious Traveler UI and the 6-step onboarding flow — matters because these are recently built but untested, and investor demos depend on them being reliable — constraint: test in a real browser session, don't assume Leaflet map + hotel photos render.\n4. Commit or discard the untracked `Kanoe_Investor_Deck_May 2026 7.key` — matters because a stray untracked binary clutters the tree and may be an intended deck update — constraint: confirm with owner whether it supersedes the existing deck before committing.\n\n# ─────────────────────────────────────────────────────────────\n# PART 3 — THE GUARDRAILS (what stops drift and repetition)\n# ─────────────────────────────────────────────────────────────\n\n## ✋ DO NOT TOUCH — load-bearing, deliberate, leave alone\n- `voiceChat: false` in the HeyGen LiveAvatar init — looks like a disabled feature but is deliberately OFF because WE own the mic pipeline (Deepgram AudioWorklet). Turning it on would double-capture audio and break turn-taking. Do not change.\n- claude-sonnet-4-5 in services/conductor.py run_general (max_tokens=300) and merge step (max_tokens=400) — looks like it contradicts the docs' \"Haiku\" claim but the code is correct and deliberate. The doc is wrong. Do not downgrade to Haiku.\n- The echo filtering / dedup / gate / barge-in logic in the voice pipeline (commit dcb5b3f applied HeyGen echo fix) — looks refactorable but is the fragile crown jewel of turn-taking. Do not \"simplify.\"\n- backend/app/services/conductor.py (NOT api/conductor.py) is the real orchestrator — do not migrate logic or assume api/conductor.py is authoritative.\n\n## 🚫 ALREADY DECIDED — do not re-open these\n| Decision | Why we chose it | What we rejected | Reversible? |\n|----------|-----------------|------------------|-------------|\n| Own the mic pipeline (Deepgram AudioWorklet), voiceChat:false | Full control of STT, echo, barge-in, turn-taking | Letting HeyGen handle voiceChat | No |\n| claude-sonnet-4-5 for conductor orchestration | Quality/latency balance for dispatch + merge | claude-haiku-4-5 (as old docs stated) | Reversible but not now |\n| Repo temporarily PUBLIC | Required for HeyGen review | Keeping private during review | Yes — make private after review |\n| Next.js App Router + FastAPI + Supabase split | Vercel frontend / Railway backend / managed Postgres w/ RLS | Monolith | No |\n\n## 💀 TRIED AND FAILED — do not suggest these again\n- Not yet established (no failed-approach history captured; this is Session #1 with no transcript).\n\n## 🧭 USER'S STANDING PREFERENCES — how they want things done\n- Start every new chat by reading docs/session_current.md and referenced files, then continue without re-explaining (per README).\n- End sessions by regenerating session_current.md via `ANTHROPIC_API_KEY=... ./scripts/end_session.sh` (auto-commits).\n- Commit messages follow conventional style: feat: / fix: / chore
---
_Generated: 2026-07-08T06:07:41.982Z

# Session State — 07/08/2026, 10:40 AM

_Auto-captured by Claude Thread_

## Summary
Session captured

{"summary": "This session focused on the Kanoe investor presentation (sasha_investor.html). The user rejected placing two new slides (Agent Architecture + Token Optimisation) as standalone deck slides and instead had them merged into a single 'Performance Architecture' slide (tdm-s11) inside the TDM deck. The session ended mid-task with two outstanding requests: (1) design an API Hub architecture where system APIs coordinate with the agents, and (2) remove the $1M fundraising boxes from BOTH the Teaser tab and the Investor Deck tab — the assistant was locating those boxes when the response was cut off.", "session_doc": "# ════════════════════════════════════════════════════════════\n# SASHA / KANOE — AI TRAVEL CONCIERGE — THREAD CONTEXT\n# The single source of truth. Read this completely before doing anything.\n# Generated: 2026-07-08 · Model: Sonnet · Session #2 · ~1100 tokens\n# ════════════════════════════════════════════════════════════\n\n## ⚡ READ THIS FIRST — 30 SECOND ORIENTATION\nKanoe is a multi-agent AI travel platform whose voice concierge \"Sasha\" runs on a HeyGen LiveAvatar with a self-owned Deepgram STT mic pipeline and 23 backend specialist agents. This session was NOT touching the voice pipeline — it was editing the investor presentation at frontend/public/sasha_investor.html. Right now there are two unfinished requests: remove the $1M fundraising boxes from BOTH the Teaser tab and the Investor Deck tab, and design/add an \"API Hub\" architecture concept — the assistant was cut off mid-search for the $1M boxes.\n\n> NEXT ACTION: Open frontend/public/sasha_investor.html and run `grep -n \"1 million\\|1M\\|\\$1,000,000\\|raise\\|Use of Proceeds\\|fundraising\" frontend/public/sasha_investor.html` to locate BOTH $1M fundraising boxes — one in the Teaser tab and one in the Investor Deck tab. Delete both box elements entirely (the user will share funding info separately). Then produce the updated file, and instruct the user to run: `cp ~/Downloads/sasha_investor.html frontend/public/sasha_investor.html && git add . && git commit -m \"Remove \\$1M fundraising boxes from Teaser + Investor Deck tabs\" && git push origin main`. Verify by re-grepping the saved file to confirm zero remaining $1M box references. Do NOT deliver a file via /mnt/user-data/outputs and tell the user to `cp` from there — that path is NOT accessible from their terminal (it failed last session); always give them the Download link + a `cp ~/Downloads/...` command.\n> DO NOT: Do not re-add the Agent Architecture and Token Optimisation content as standalone investor-deck slides — the user explicitly rejected that placement. That content now lives as ONE combined 'Performance Architecture' slide (tdm-s11) inside the TDM deck. Leave it there.\n\n# ─────────────────────────────────────────────────────────────\n# PART 1 — WHAT THIS IS (the unchanging core)\n# ─────────────────────────────────────────────────────────────\n\n## THE ESSENCE — never let this drift\nKanoe is a multi-agent AI travel concierge platform. The one thing it must always do well: deliver a natural, low-latency VOICE conversation with an on-screen video avatar (Sasha) that dispatches user requests to 23 specialist agents (golf, visa, restaurants, transfers, etc.) and speaks answers back through the HeyGen avatar. The crown jewel and most fragile part is the voice pipeline: HeyGen LiveAvatar (video + TTS, `voiceChat: false` so WE own the mic) → Deepgram nova-3 AudioWorklet STT → conductor → response → avatar.repeat(). Any change that degrades turn-taking, echo suppression, or gate/barge-in behavior compromises the essence and must be rejected. Secondary but active right now: the investor-facing presentation (sasha_investor.html) that pitches this to funders.\n\n## WHO IT'S FOR\nRepo owner/developer is tylerwarren-droid (terminal prompt: tylerwarren@Mac-mini-van-Tyler, project at ~/Projects/sasha-travel). End users are luxury/high-end travelers using demos like /vietnam and /phuquoc. There is a live investor-facing track (investor.kanoe.ai → sasha_investor.html) — polish and reliability matter for fundraising. HeyGen is currently reviewing the repo (made public for that review). Technical level: developer-competent, works from a Mac Mini, runs git manually, downloads files from Claude's chat rather than accessing container paths. He has clear opinions on slide placement and will redirect content he doesn't like.\n\n# ─────────────────────────────────────────────────────────────\n# PART 2 — WHAT'S TRUE RIGHT NOW (the current reality)\n# ─────────────────────────────────────────────────────────────\n\n## CURRENT STATE — what actually exists vs what's aspirational\n- WORKING NOW (verified): Live URLs deployed — investor.kanoe.ai, demo.kanoe.ai, sasha-heygen.vercel.app/vietnam, sasha-heygen.vercel.app/phuquoc, backend at sasha-travel-production.up.railway.app. Voice pipeline documented as functioning (tap-to-start → HeyGen avatar → Deepgram STT → conductor → avatar.repeat, with echo filtering, dedup, gate/barge-in). 23 backend agent service modules exist (backend/app/services/*_agent.py). DB writes wired (commit dcb5b3f). The investor presentation sasha_investor.html has a Teaser tab, an Investor Deck tab, and a TDM deck (now 11 slides).\n- BUILT BUT UNVERIFIED: The new 'Performance Architecture' slide (tdm-s11) added this session — combines agent orchestration flow (left) + three-level intelligence model & LLM routing cost table (right). It was written and delivered but NOT confirmed deployed/reviewed live. The /vietnam2 Luxurious Traveler UI with Leaflet map (commits 6461b4c/990f01f). Onboarding flow (frontend/app/onboarding, 6 steps).\n- ASPIRATIONAL / NOT BUILT: The 'API Hub' architecture — the user asked to design an architecture where system APIs engage with the agents and coordinate amongst themselves; this is NOT yet designed or built, only requested at end of session. Removing the two $1M fundraising boxes — requested, NOT yet done. Making the repo private (still PUBLIC for HeyGen review). AgAPI / Dataset Licensing / TDM / MOR corpus investor-deck pillars are pitch/strategy, not necessarily shipped code.\n\n## LAST SESSION — what just happened\n- We worked on: Editing the investor presentation sasha_investor.html — specifically adding technical slides and beginning fundraising-box removal.\n- We completed: Created a combined 'Performance Architecture' slide as tdm-s11 in the TDM deck (agent orchestration flow + three-level intelligence model + LLM routing cost table), updated the TDM nav counter, and delivered the file for download.\n- We changed: frontend/public/sasha_investor.html (added tdm-s11, updated TDM slide count/nav). Earlier in-session we first added two standalone deck slides (07 Agent Architecture, 08 Token Optimisation) but the user rejected that and we moved the content into tdm-s11 instead.\n- We left off mid-: Locating the two $1M fundraising boxes to delete them from the Teaser tab and Investor Deck tab. The assistant ran a grep to find them and was cut off (\"Claude couldn't finish this response\") before editing. The API Hub architecture request was also raised in the same message and is completely untouched.\n\n## OPEN TASKS — ranked, with the WHY\n1. Remove the two $1M fundraising boxes from the Teaser tab AND the Investor Deck tab in sasha_investor.html — matters because the user wants funding info shared privately/separately, not on the public deck HeyGen is reviewing — constraint: delete BOTH, verify with grep, deliver via Download + `cp ~/Downloads/...`.\n2. Design an API Hub architecture — matters because the user wants to pitch a system where existing APIs engage with the agents and coordinate amongst themselves; likely becomes new presentation content. Deliver a design first, then ask where it should live before adding slides (he cares about placement).\n3. Reconcile docs vs code: conductor lives at backend/app/services/conductor.py (docs may claim api/conductor.py), and the code uses claude-sonnet-4-5 (docs may claim Haiku) — matters because a cold Claude must not 'fix' code to match wrong docs. Fix docs/new_chat_handoff.md, not the code.\n4. Make the repo private once HeyGen review completes — matters for security (currently PUBLIC).\n\n# ─────────────────────────────────────────────────────────────\n# PART 3 — THE GUARDRAILS (what stops drift and repetition)\n# ─────────────────────────────────────────────────────────────\n\n## ✋ DO NOT TOUCH — load-bearing, deliberate, leave alone\n- The voice pipeline (SashaAvatar.tsx, VoiceButton.tsx, pcm-capture.js, deepgram_service.py, HeyGen `voiceChat: false`) — looks like it could be simplified/refactored but is deliberately hand-tuned for echo suppression, dedup, and gate/barge-in. Do not touch this session; work is on the investor HTML.\n- The claude-sonnet-4-5 model choice in backend/app/services/conductor.py (run_general max_tokens=300, merge step max_tokens=400) — looks like it should be Haiku per some docs, but the CODE is deliberately Sonnet. The doc is wrong, not the code. Do not change the code.\n- tdm-s11 'Performance Architecture' slide placement inside the TDM deck — looks like technical content that 'belongs' in the main investor deck as its own slide, but the user deliberately moved it INTO the TDM deck. Leave it there.\n\n## 🚫 ALREADY DECIDED — do not re-open these\n| Decision | Why we chose it | What we rejected | Reversible? |\n|----------|-----------------|------------------|-------------|\n| Agent Architecture + Token Optimisation content lives as ONE combined 'Performance Architecture' slide (tdm-s11) inside the TDM deck | User said the TDM is the right home for that technical detail | Two standalone slides (07 & 08) between Competitive Landscape and Financial Projections | No |\n| $1M funding info removed from the public presentation | User will share funding details separately/privately | Keeping the $1M raise boxes on Teaser + Investor Deck tabs | Yes |\n| We own the mic pipeline (HeyGen voiceChat:false + Deepgram STT) | Control over echo/turn-taking/latency | Letting HeyGen handle voice chat natively | No |\n\n## 💀 TRIED AND FAILED — do not suggest these again\n- Telling the user to `cp /mnt/user-data/outputs/sasha_investor.html ...` — failed with \"No such file or directory\" because the container output path is NOT accessible from his Mac terminal. Always give the Download link and a `cp ~/Downloads/sasha_investor.html ...` command instead.\n- Placing the technical slides as standalone investor-deck slides — the user rejected it; do not re-suggest.\
---
_Generated: 2026-07-08T08:40:50.967Z
